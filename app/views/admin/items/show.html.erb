<div class="mt-3">
<h2><span class="bg-secondary text-white">商品詳細</span></h2>

<div class="container text-center">
<div class="row row-cols-2">
  <div class="col">
    <% if @item.image.attached? %>
      <%= image_tag @item.image, size: "200x200" %>
    <% else %>
      <%= image_tag 'no_image_cake', size: "200x200" %>
    <% end %>
  </div>

  <div class="col">
    <table>
      <tbody>
        <tr>
          <th>商品名</th>
          <td><%= @item.name %></td>
        </tr>
      </tbody>
        <tr>
          <th>商品説明</th>
          <td><%= @item.introduction %></td>
        </tr>
        <tr>
          <th>税込価格<br>
          （税抜価格）</th>
          <td><%= number_with_delimiter(sprintf("%.0f", @item.with_tax_price), delimiter: ',') %>（<%= @item.price %>）円</td>
        </tr>
      </tbody>
    </table>
  </div>
  <div class="row"></div>
<div class="row">
  <%= link_to "編集する", admin_edit_item_path(@item), method: :get, class: 'btn btn-success' %>
</div>
