<%- if controller_name != 'sessions' %>
  <div>
    <h6><span class="bg-secondary text-white">既に登録済みの方</span></h6>

    <p>
  <%= link_to "こちら", new_session_path(resource_name) %>
  からログインしてください。
    </p>
  </div>
<% end %>

<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <div>

    <h6><span class="bg-secondary text-white">会員登録がお済みではない方</span></h6>
    <p>
      <%= link_to "こちら", new_registration_path(resource_name) %>
      から新規登録を行ってください。
    </p>
  </div>
<% end %>

